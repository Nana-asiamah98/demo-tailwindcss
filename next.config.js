/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.icons8.com', 'em-content.zobj.net', 'unsplash.com'],
  },
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
